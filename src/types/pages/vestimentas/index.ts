export interface CategoriesType {
    id: number,
    category: string,
    selected: boolean
}

export interface ProductList {
    id: number,
    name: string,
    pricing: number,
    image: string
}

